<div id="packet_form">
  <%= form_for @packet, url: user_packets_path(params[:user_id]) do |f| %>
    <%= f.label :name, "nazwa paczki" %>
    <%= @packet.error(:name) %>
    <%= f.text_field :name %>
    <%= f.submit "dodaj paczkę" %>
    <%= link_to "Anuluj", "#", onclick: "hide_form(#{"packet_form"})", class: "show_or_hide_form" %>
  <% end %>
</div>

<% if @packet.errors.any? %>
  <script type="text/javascript">
    show_packet_form();
  </script>
<% end %>
