<div class="container jumbotron">
  <div class="flex-container">
    <div>
      <h3 class="object">Co usłyszałeś?</h3>
      <input class="object btn btn-primary btn-sm"
      onclick='responsiveVoice.speak("<%= @answer.english %>");' type='button'
      value='🔊 Odtwórz' id='autoplay' />
      <br />
    </div>
  </div>
  <br />
  <div class="flex-container">
    <div>
      <%= form_tag manager_words_learning_session_path(params[:id]), method: "GET" do %>
        <% @words.each do |word| %>
          <%= radio_button_tag :guess, word.english %>
          <%= word.english %>
          <br />
        <% end %>
        <%= hidden_field_tag :index, params[:index] %>
        <%= hidden_field_tag :answer, @answer.english %>
        <%= hidden_field_tag :type, params[:type] %>
        <%= hidden_field_tag :correctness, "" %>
        <%= button_tag "sprawdź", onclick: "check_type_1()",
        id: "js_activation_button", class: "btn btn-primary btn-sm" %>
        <p id="feedback_info"></p>
        <p id="after_guess">Po polsku: <%= @answer.polish %></p>
        <%= submit_tag "Dalej", class: "next_after_check btn btn-primary btn-sm" %>
      <% end %>
    </div>
  </div>
</div>
