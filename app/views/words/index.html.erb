Jesteś aktualnie w paczce słówek: <%= @packet.name %>
<button><%= link_to "Zmień paczkę", user_packets_path(params[:user_id]) %></button><br>

<%#= radio_button_tag  %>
<% if @packet.words.any? %>
  <table>
    <tr>
      <th>polski</th>
      <th> polskie synonimy</th>
      <th>angielski</th>
      <th>angielskie synonimy</th>
      <th>dźwięk</th>
      <th>akcja</th>
    </tr>
    <% @packet.words.each do |word| %>
    <tr>
      <td><%= word.polish %></td>
      <td><%= word.polish_synonyms %></td>
      <td><%= word.english %></td>
      <td><%= word.english_synonyms %></td>
      <td><input onclick='responsiveVoice.speak("<%= word.english %>");' type='button' value='🔊 Play' /></td>
      <td>
        <%= link_to "edytuj", edit_user_packet_word_path(params[:user_id], params[:packet_id], id: word.id) %>
        <%= link_to "usuń", user_packet_word_path(params[:user_id], params[:packet_id], id: word.id), method: "delete", data: { confirm: "Jesteś pewien, że chcesz usunąć słowo z paczki?" } %>
      </td>
    </tr>
    <% end %>
  </table>
  <button><%= link_to "ucz się nowych słowek(w wybranym przebiegu)", '#' %></button>
  <button><%= link_to "powtórki na dziś(w wybranym przebiegu)", '#' %></button>
<% else %>
  Paczka <%= @packet.name %> nie ma jeszcze żadnych słów.
<% end %>
<button><%= link_to "dodaj słowo", new_user_packet_word_path(params[:user_id], params[:packet_id]) %></button>
